[project]
name = "pyflecs"
version = "0.1.0"
description = "A python binding for flecs using ctypes"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bidict>=0.23.1",
    "ctypesgen>=1.1.1",
    "ruff>=0.12.4",
]

[tool.setuptools.package-data]
pyflecs = ["**/*.dylib"]
